import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.raned.quizzgame',
  appName: 'QuizzGame',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
